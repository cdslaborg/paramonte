classdef Count_class

    properties
        compact = []    % number of unique (weighted) points in the chain
        verbose = []    % number of points (weight=1) in the MCMC chain
        resized = []    % size of the allocations for the Chain components
    end

end